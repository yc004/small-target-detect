# 基于改进 YOLO26 的小目标交通标志实时检测 — 项目设计方案

## 1. 项目概述
- **题目**：基于改进 YOLO26 的小目标交通标志实时检测
- **领域**：智能交通与智慧城市
- **基线模型**：YOLO26s（ultralytics 官方最新检测模型，2025 年发布）
- **目标**：在 TT100K 数据集上系统性改进 YOLO26，针对小目标检测进行专项优化，通过 6 阶段渐进式实验验证每个改进点的有效性，在保持实时推理的前提下显著提升小目标检测精度。

### 为什么选择 YOLO26？
YOLO26 是 ultralytics 在 YOLOv8/v10/v11/v12 之后的下一代检测架构，引入三项关键升级：
- **C3k2 模块**：替代 YOLOv8 的 C2f，通过 Kernel Selection 机制实现多尺度特征自适应融合，对小目标的细粒度特征提取更有效
- **C2PSA 注意力**：在 Backbone 末端内置 Cross-Stage Partial Self-Attention，提供全局上下文建模，优于事后插入的 ECA/SE 等轻量注意力
- **端到端检测模式**（end2end + reg_max=1）：去掉 DFL 分布预测分支，直接回归边界框，减少小目标定位的量化误差

### 创新点总览（6 阶段渐进式）

```
Stage 1 (基线)        → Stage 2 (+P2)       → Stage 3 (+ARF-Head)
  官方 YOLO26s            官方 P2 高分辨率头       自适应感受野检测头
  建立性能基线            首次提升小目标召回         尺度感知的特征提取

Stage 4 (+BCEM)       → Stage 5 (+HJ-Loss)   → Stage 6 (集成+交付)
  双向上下文增强模块       分层联合损失函数          消融实验 + Demo
  前景/背景判别增强        小目标定位精度提升         完整系统交付
```

| 阶段 | 改进点 | 创新程度 | 技术来源 |
|:---:|--------|:---:|------|
| 1 | YOLO26s 基线训练 | — | ultralytics 官方 |
| 2 | 官方 P2 高分辨率检测层 | — | ultralytics 官方 |
| 3 | **ARF-Head** 自适应感受野检测头 | ⭐⭐⭐ | **原创设计** |
| 4 | **BCEM** 双向上下文增强模块 | ⭐⭐⭐⭐ | **原创设计** |
| 5 | **HJ-Loss** 分层联合损失 (WIoUv3 + NWD + 动态权重) | ⭐⭐⭐⭐ | **原创设计** |
| 6 | Soft-NMS + 系统集成 | ⭐⭐ | ICCV 2017 + 原创集成 |

### 与旧方案（YOLOv8 版）的本质区别

| 维度 | 旧方案 (YOLOv8) | 新方案 (YOLO26) |
|------|:---|:---|
| 基线 | YOLOv8s (2023) | YOLO26s (2025) |
| 注意力 | ECA — 1D 卷积通道注意力 (CVPR 2020) | BCEM — 原创双向上下文增强 + YOLO26 自带 C2PSA |
| 检测头 | 简单堆叠 P2 层 | ARF-Head — 尺度感知的自适应感受野 |
| 损失函数 | 默认 CIoU Loss | HJ-Loss — WIoUv3 + NWD 分层联合损失 |
| 原创性 | 全为已知方法堆叠 | ARF-Head / BCEM / HJ-Loss 均为原创设计 |
| 实验价值 | 消融实验验证已知方法 | 消融实验验证原创方法的有效性 |

---

## 2. 环境与依赖
- Python >= 3.10
- PyTorch >= 2.0.0
- ultralytics >= 8.4.0（内置 YOLO26）
- torchvision, opencv-python, albumentations, pycocotools
- numpy, matplotlib, seaborn, pyyaml, tqdm, pillow

---

## 3. 数据集
- **来源**：TT100K（Tsinghua-Tencent 100K）中国交通标志数据集
- **类别选取**（5 类小目标交通标志）：

| Class ID | TT100K Key | 中文名称 | 说明 |
|:---:|------|------|------|
| 0 | i2 | 限速5 | 最低限速标志，极小尺寸 |
| 1 | i4 | 限速30 | 常见限速标志 |
| 2 | i5 | 限速40 | 常见限速标志 |
| 3 | pne | 禁止驶入 | 禁令标志（替代原方案中的 io，该类别在 TT100K 中不存在） |
| 4 | p10 | 禁止行人通行 | 禁令标志，样本量最少 |

- **数据划分**：Train : Val : Test = 7 : 2 : 1
- **图像分辨率**：2048×2048（原始），训练时统一 resize 至 640×640
- **小目标定义**：像素面积 < 32×32 的目标（COCO 标准）
- **预期数据量**：~3100 张标注图片，~150MB（压缩后）

---

## 4. 模型改进详细设计

### 4.1 Stage 1: YOLO26s 基线

直接使用官方 `yolo26s.yaml` 进行训练，不做任何修改。

**目的**：
- 建立性能基线，记录 mAP@0.5、mAP@0.5:0.95、AP_S、FPS 等关键指标
- 与后续各阶段进行严格对比
- 验证数据流水线和训练流程的正确性

**关键配置**：
- model: `yolo26s.yaml`
- pretrained: True (COCO 预训练权重)
- epochs: 100, imgsz: 640, batch: 16, device: CUDA GPU
- optimizer: auto, lr0: 0.01, cos_lr: True

### 4.2 Stage 2: 官方 YOLO26s-P2

使用 ultralytics 官方提供的 `yolo26s-p2.yaml`，该配置在 YOLO26 的 Neck 中增加了 P2/4 高分辨率检测层（160×160 特征图），专为小目标设计。

**原理**：YOLO26 默认有 P3(80×80)、P4(40×40)、P5(20×20) 三个检测头，stride 分别为 8/16/32。对于 640×640 输入，P3 特征图每个 cell 对应 8×8 像素区域。如果目标仅 16×16，在 P3 上仅占 2×2 个 cell。P2 层 stride=4，同样 16×16 目标在 P2 上占 4×4 个 cell，特征表达能力提升 4 倍。

**P2 层 Neck 结构**（官方实现）：
- 在 FPN 自顶向下路径中增加一次 2× 上采样，与 Backbone P2 (stride=4) 特征拼接
- 在 PAN 自底向上路径中增加对应的 2× 下采样
- 最终 4 个检测头：P2(160×160)、P3(80×80)、P4(40×40)、P5(20×20)

**预期**：AP_S 提升 ≥ 5%，参数量增加约 0.6M，FPS 下降约 15%。

### 4.3 Stage 3: ARF-Head（自适应感受野检测头）⭐ 原创

**动机**：YOLO26 的 4 个检测头使用完全相同的结构（C3k2 + Detect），没有考虑不同尺度检测层对感受野需求的差异。P2 层检测极小目标时需要适度扩大感受野以捕获上下文（帮助区分目标与噪声），而 P5 层检测大目标时感受野已经足够甚至过大。

**ARF-Head 设计**：
- 在每个检测层前插入一个轻量级的 **Adaptive Receptive Field (ARF)** 模块
- ARF 模块包含并行的多分支空洞卷积，各分支的空洞率随检测层尺度自适应调整
- P2 层：空洞率 [1, 3, 5] — 适度扩大感受野以捕获小目标周围上下文
- P3 层：空洞率 [1, 2, 3] — 轻微扩大
- P4/P5 层：空洞率 [1, 1, 1] — 标准卷积（大目标已有足够感受野）
- 多分支输出通过可学习的权重进行 softmax 融合
- 模块使用深度可分离卷积以控制计算开销

**ARF 模块结构**：
```
Input (C × H × W)
    │
    ├─→ DWConv 3×3, dilation=1 ──→ 1×1 Conv ──┐
    ├─→ DWConv 3×3, dilation=d1 ──→ 1×1 Conv ──┤
    ├─→ DWConv 3×3, dilation=d2 ──→ 1×1 Conv ──┤
    │                                           │
    └─→ AdaptiveAvgPool2d(1) → FC → softmax ──→ weighted sum → Output
```
其中 (d1, d2) 根据检测层自适应配置：P2=(3,5), P3=(2,3), P4/P5=(1,1)。

**创新点**：
- 首次针对不同检测层设计**尺度感知的自适应感受野**
- 使用可学习的 softmax 融合替代简单的 feature addition
- 深度可分离卷积降低各分支开销，但 pointwise 卷积仍贡献主要参数量

**预期**：AP_S 提升 ≥ 2%，参数量增加约 +1.5M（YOLO26s-P2 的 9.77M → 11.28M，增幅 15%）。

### 4.4 Stage 4: BCEM（双向上下文增强模块）⭐ 原创

**动机**：小目标交通标志容易与背景混淆（如路牌上的文字、建筑边缘等）。YOLO26 的 C2PSA 仅提供全局自注意力，缺乏对**前景/背景边界的显式建模**。PANet 的跳跃连接直接将 Backbone 浅层特征拼接到 Neck，但这些浅层特征包含大量背景噪声。

**BCEM 设计**：
- 插入位置：Backbone 到 Neck 的每条跳跃连接之前
- 结构：双分支并行处理

```
Backbone Feature (C × H × W)
    │
    ├─→ Channel Branch ────────────────────────┐
    │   GAP → Conv1d(k) → Sigmoid              │
    │   (通道注意力，保留重要通道)               │
    │                                          ├─→ Refined Feature
    └─→ Spatial Branch ────────────────────────┘
        Conv 1×1 → DWConv 7×7 → Sigmoid
        (空间注意力，增强前景/抑制背景)
```

**关键设计决策**：
- 空间分支使用 7×7 大核深度卷积，提供足够的感受野进行前景/背景判别
- 通道分支使用类似 ECA 的 1D 卷积，自适应核大小 `k = |log2(C)/2 + 0.5|_odd`
- 两个分支的输出通过 element-wise multiplication 融合（而非加法），确保双重确认才增强
- 使用残差连接：`Output = Input + Input × Channel_Attn × Spatial_Attn`

**与 YOLO26 自带 C2PSA 的关系**：
- C2PSA 在 Backbone 末端提供全局级别的自注意力（位置：SPPF 之后）
- BCEM 在跳跃连接处提供局部级别的前景/背景增强（位置：Backbone → Neck 跳跃连接前）
- 二者互补：C2PSA 负责全局语义，BCEM 负责局部细节

**创新点**：
- 首次在检测 Neck 跳跃连接中引入**双向（通道+空间）上下文增强**
- 乘法融合机制确保通道和空间维度双重确认
- 针对小目标场景优化的大核空间注意力（7×7）

**预期**：mAP@0.5 提升 ≥ 1.5%，AP_S 提升 ≥ 2%，参数量增加约 +0.09M。

### 4.5 Stage 5: HJ-Loss（分层联合损失函数）⭐ 原创

**动机**：IoU-based Loss（如 CIoU、WIoU）对小目标的微小位置偏移极度敏感。一个 16×16 的目标偏移 2 个像素，IoU 可能从 0.8 骤降至 0.5。而大目标（如 100×100）偏移 2 个像素影响极小。此外，不同检测层负责不同尺度的目标，但 YOLO26 对所有层使用相同的损失函数和权重。

**HJ-Loss 设计**：

**组件 1：主损失 — Wise-IoU v3**
- 替换默认的 CIoU Loss
- WIoUv3 通过动态非单调聚焦机制 (dynamic non-monotonic FM) 自动关注中等质量的 anchor，抑制极端离群样本
- 公式：`L_WIoUv3 = r × R_WIoU × L_IoU`，其中 `r = β / (δ × α^(β-δ))` 是动态梯度增益

**组件 2：辅助损失 — NWD（Normalized Wasserstein Distance）**
- 仅用于 P2 和 P3 检测层（小目标层）
- NWD 将边界框建模为高斯分布，计算 Wasserstein 距离，对小尺度偏移不敏感
- 公式：`NWD(N_a, N_b) = exp(-√W_2^2(N_a, N_b) / C)`，其中 C 是归一化常数
- 平滑且尺度不变的定位监督

**组件 3：分层自适应权重**
- P2 和 P3 层：`L = WIoUv3 + λ_nwd × NWD`（λ_nwd = 0.3）
- P4 和 P5 层：`L = WIoUv3`（不加 NWD，避免对大目标的噪声）
- 分类损失保持不变（BCE）

**总损失函数**：
```
L_total = λ_cls × L_cls + Σ_{layer∈P2,P3} (WIoUv3 + 0.3×NWD) + Σ_{layer∈P4,P5} WIoUv3
```

**创新点**：
- 首次提出**检测层级别**的差异化损失函数设计
- WIoUv3 + NWD 的联合使用方式：WIoUv3 提供动态样本加权，NWD 提供尺度鲁棒的定位监督
- NWD 仅作用于小目标层（P2/P3），避免对大目标引入不必要的噪声

**预期**：AP_S 提升 ≥ 3%，小目标定位精度（IoU 阈值 > 0.75 时的 AP）显著提升。

### 4.6 Stage 6: 推理优化与系统集成

**内容**：
- **Soft-NMS**：将标准 NMS 替换为 Gaussian Soft-NMS（σ=0.5），降低密集小目标场景下的误杀率
- **多尺度 TTA**（可选）：推理时使用 [0.83×, 1.0×, 1.17×] 三个尺度，结果用 WBF 融合
- **ONNX 导出**：导出 ONNX 模型用于部署
- **完整消融实验**：逐一验证各阶段贡献
- **实时 Demo**：基于 OpenCV 的摄像头实时检测

---

## 5. 项目文件结构（最终形态）

```
small_target_detect/
├── data/
│   ├── TT100K/                      # 原始数据集
│   ├── processed/                   # YOLO 格式转换后的数据
│   │   ├── dataset.yaml
│   │   ├── images/{train,val,test}/
│   │   └── labels/{train,val,test}/
│   ├── prepare_dataset.py           # TT100K JSON → YOLO 格式
│   └── extract_dataset.py           # 从预打包 ZIP 提取
├── models/
│   ├── yolov8s_p2.yaml              # (旧版，保留)
│   ├── yolo26s_p2_arf.yaml          # Stage 3: + P2 + ARF-Head
│   ├── yolo26s_p2_arf_bcem.yaml     # Stage 4: + P2 + ARF-Head + BCEM
│   ├── arf_head.py                  # ARF-Head 模块实现
│   ├── bcem.py                      # BCEM 模块实现
│   ├── hj_loss.py                   # HJ-Loss 分层联合损失
│   └── model_builder.py             # 模型构建工具
├── utils/
│   ├── soft_nms.py                  # Soft-NMS 实现
│   ├── visualization.py             # 检测结果可视化
│   └── metrics.py                   # 小目标专项评估指标
├── configs/
│   ├── stage1_baseline.yaml         # YOLO26s 基线训练配置
│   ├── stage2_p2.yaml               # YOLO26s-P2 训练配置
│   ├── stage3_arf_head.yaml         # + ARF-Head
│   ├── stage4_bcem.yaml             # + BCEM
│   ├── stage5_hjloss.yaml           # + HJ-Loss
│   └── stage6_full.yaml             # 最终集成模型
├── experiments/
│   ├── stage1_baseline/
│   ├── stage2_p2/
│   ├── stage3_arf_head/
│   ├── stage4_bcem/
│   ├── stage5_hjloss/
│   └── stage6_full/
├── scripts/
│   ├── train.py                     # 统一训练入口
│   ├── detect.py                    # 图片/视频推理
│   ├── eval.py                      # COCO 评估 + 尺寸分组
│   ├── demo.py                      # 实时摄像头演示
│   └── analyze_data.py              # 数据集统计分析
├── docs/
│   ├── generate_report.py           # Word 报告生成
│   ├── 作品报告_*.docx
│   └── 作品报告模板.docx
├── pyproject.toml
├── requirements.txt
└── README.md
```

---

## 6. 实验设计与评估

### 6.1 评估指标
- mAP@0.5, mAP@0.5:0.95（COCO 标准）
- AP_S (面积 < 32×32 px), AP_M (32×32 ~ 96×96 px), AP_L (> 96×96 px)
- Precision, Recall, F1-score
- 推理速度 FPS（GPU/CPU）
- 参数量 (#Params)、FLOPs

### 6.2 消融实验矩阵

| 实验 | 模型 | P2 | ARF-Head | BCEM | HJ-Loss | mAP@0.5 | mAP@0.5:0.95 | AP_S | FPS |
|:---:|------|:--:|:---:|:--:|:--:|:---:|:---:|:---:|:---:|
| A | YOLO26s (基线) | ✗ | ✗ | ✗ | ✗ | ? | ? | ? | ? |
| B | + 官方 P2 | ✓ | ✗ | ✗ | ✗ | ? | ? | ? | ? |
| C | + ARF-Head | ✓ | ✓ | ✗ | ✗ | ? | ? | ? | ? |
| D | + BCEM | ✓ | ✓ | ✓ | ✗ | ? | ? | ? | ? |
| E | + HJ-Loss | ✓ | ✓ | ✓ | ✓ | ? | ? | ? | ? |

**贡献度分析**：
- ΔB-A = P2 检测层的贡献
- ΔC-B = ARF-Head 的贡献
- ΔD-C = BCEM 的贡献
- ΔE-D = HJ-Loss 的贡献

---

## 7. 训练策略

- **优化器**：SGD，momentum=0.937，weight_decay=0.0005
- **学习率**：lr0=0.01，cosine annealing 调度
- **预热**：3 epoch 线性预热
- **早停**：patience=20 epoch
- **数据增强**：Mosaic (1.0), 随机缩放 (0.5), 平移 (0.1), HSV 抖动, 水平翻转 (0.5)
- **Mosaic 策略**：最后 10 epoch 关闭 Mosaic 以提升收敛质量
- **Batch Size**：Baseline=16, P2=8（因 P2 层 FLOPs 增加约 25%）

---

## 8. 预期成果

| 指标 | YOLOv8s (旧基线) | YOLO26s (新基线) | YOLO26s+Full (最终) |
|------|:---:|:---:|:---:|
| mAP@0.5 | ~0.82 | ~0.85 (更强基线) | **≥ 0.90** |
| mAP@0.5:0.95 | ~0.55 | ~0.58 | **≥ 0.63** |
| AP_S | ~0.45 | ~0.48 | **≥ 0.58** (+10pp) |
| FPS (RTX 3060) | ~120 | ~130 (更高效) | **≥ 80** (仍实时) |
| 参数量 | 11.17M | 9.77M | **~11.4M** |

**核心亮点**：
- 3 项原创模块设计（ARF-Head, BCEM, HJ-Loss），非简单堆叠已知方法
- 6 阶段渐进式实验，每阶段可独立验证
- 从 Backbone 到 Neck 到 Head 到 Loss 的全链路优化
- 基线从 YOLOv8 升级到 YOLO26，紧跟 2025 年最新进展

---

## 9. 关键技术风险与对策

| 风险 | 影响 | 概率 | 对策 |
|------|------|:---:|------|
| ARF-Head 空洞卷积增加显存 | batch 需进一步减小 | 中 | 使用深度可分离卷积；仅在 P2/P3 层加 ARF |
| BCEM 乘法融合导致梯度消失 | 训练不稳定 | 低 | 残差连接 + 初始化学输出接近 1 |
| NWD Loss 与大目标不兼容 | P4/P5 精度下降 | 低 | 仅对 P2/P3 层使用 NWD（设计已规避） |
| YOLO26 预训练权重不兼容 | 需从头训练 | 低 | ultralytics 官方提供 COCO 预训练权重 |
| P2 层 + ARF 导致显存超限 | OOM | 中 | 减小 batch 至 4 + gradient accumulation |

---

## 10. 里程碑规划

```
阶段 1 (1-2天): 环境配置 + YOLO26s 基线训练 + 基线评估
阶段 2 (1-2天): YOLO26s-P2 训练 + P2 贡献量化
阶段 3 (2-3天): ARF-Head 模块开发 + 训练 + 验证
阶段 4 (2-3天): BCEM 模块开发 + 训练 + 验证
阶段 5 (1-2天): HJ-Loss 实现 + 训练 + 验证
阶段 6 (2-3天): 消融实验 + Demo + 文档 + 报告
─────────────────────────────────────────────
总计: 9-15 天
```
