# 小目标交通标志检测与识别系统 - 项目设计稿

## 1. 项目概述
- **题目**：基于改进 YOLOv8 的小目标交通标志实时检测
- **领域**：智能城市与交通
- **目标**：在 TT100K 数据集（子集）上训练一个改进的 YOLOv8 模型，优化小目标检测性能，提供实时推理和可视化。
- **改进点**：
  - 增加专用于小目标的 P2 检测层
  - 在 Neck 中插入高效通道注意力（ECA）
  - 使用 Copy-Paste 数据增强
  - 采用 Soft-NMS 后处理

## 2. 环境与依赖
- Python 3.10+
- PyTorch 2.0+ (CUDA 11.8)
- ultralytics>=8.0.0
- torchvision, opencv-python, albumentations, pycocotools

## 3. 数据集
- **来源**：TT100K 数据集，只取以下 5 类小目标标志：
  - `i2` (限速5), `i4` (限速30), `i5` (限速40), `io` (禁止驶入), `p10` (禁止行人通行)
- **处理**：
  - 从原始标注转换为 YOLO 格式（`class_id cx cy w h` 归一化）
  - 按 7:2:1 划分 train/val/test
  - 图像分辨率保持 2048×2048 或下采样至 1280×1280（训练可选）
- **最终数据量**：约 2000 张图片，压缩后约 150MB

## 4. 模型改进详细设计

### 4.1 增加 P2 检测层
修改 YOLOv8 的 `yaml` 配置文件，在 Neck 后添加一个更高分辨率的输出头 P2（尺寸 160×160）。  
- 调整 FPN 输入：将第 2 层骨干特征（stride=4）引入 Neck，并与 P3 的上采样进行融合。
- 相应的 Anchor 分配：新增一个检测尺度，预设小尺寸 anchor（使用 Anchor-free 的 YOLOv8 则无需手动设 anchor）。

### 4.2 高效通道注意力（ECA）
在 Neck 的每个 C2f 模块后插入 ECA 层，定义为：
```python
class ECA(nn.Module):
    def __init__(self, channels, k_size=3):
        super().__init__()
        self.avg_pool = nn.AdaptiveAvgPool2d(1)
        self.conv = nn.Conv1d(1, 1, kernel_size=k_size, padding=k_size//2, bias=False)
        self.sigmoid = nn.Sigmoid()

    def forward(self, x):
        # x: (B, C, H, W)
        y = self.avg_pool(x).squeeze(-1).transpose(-1, -2)  # (B, 1, C)
        y = self.conv(y).transpose(-1, -2).unsqueeze(-1)     # (B, C, 1, 1)
        return x * self.sigmoid(y)
```
插入位置：在每个检测层输出之前。

### 4.3 Copy-Paste 增强
在训练数据加载中，以 0.5 概率执行：
- 从同一 batch 的另一张图中随机选取小目标边界框，复制其像素，随机缩放 0.8~1.2 倍，粘贴到当前图像的空旷区域（不与其他目标严重重叠）。
- 同时更新标签框。

### 4.4 Soft-NMS
将 YOLOv8 预测阶段的 NMS 替换为 Soft-NMS（降低重叠框的置信度而非删除），具体使用 `torchvision.ops.batched_nms` 的替代实现或手动编写。

## 5. 项目文件结构
```
traffic-sign-detection/
├── data/
│   ├── TT100K/              # 解压后的数据集
│   └── prepare_dataset.py   # 生成 YOLO 标签及划分
├── models/
│   ├── yolov8s_p2_eca.yaml  # 改进模型配置文件
│   ├── attention.py         # ECA 实现
│   └── copy_paste.py        # 数据增强实现
├── utils/
│   ├── soft_nms.py
│   ├── visualization.py
│   └── metrics.py
├── train.py                 # 训练入口
├── detect.py                # 图片/视频推理
├── demo.py                  # 实时摄像头演示
├── requirements.txt
└── README.md
```

## 6. 核心模块接口

### 6.1 模型配置 (`models/yolov8s_p2_eca.yaml`)
基于 YOLOv8s 的默认 yaml，修改 `head` 部分，添加 P2 层，并在每个输出层添加 `ECA` 标记（可在模型构建时解析）。

### 6.2 模型构建 (`models/build.py`)
```python
from ultralytics.nn.tasks import DetectionModel
def create_model(cfg='yolov8s_p2_eca.yaml'):
    model = DetectionModel(cfg, ch=3, nc=5)
    # 插入 ECA 模块
    # 遍历 model.model 找到特定的层并替换或追加
    return model
```

### 6.3 数据增强 (`models/copy_paste.py`)
```python
def copy_paste_augmentation(images, targets, p=0.5):
    # images: (B,3,H,W) tensor
    # targets: list of (N,5) tensor [cls, x1,y1,x2,y2] (绝对坐标)
    # 返回增强后的 images, targets
    pass
```

### 6.4 训练脚本 (`train.py`)
```python
from ultralytics import YOLO
model = YOLO('models/yolov8s_p2_eca.yaml')
model.train(data='data/TT100K/dataset.yaml', 
            epochs=100, 
            imgsz=1280, 
            batch=8,
            augment=True,
            ...)
```

### 6.5 推理与 Soft-NMS (`detect.py`)
```python
from ultralytics import YOLO
model = YOLO('runs/train/weights/best.pt')
results = model.predict(source='test.jpg', imgsz=1280, conf=0.25, iou=0.45)
# 后处理替换成 Soft-NMS 可通过 model.predict 的参数或自定义后处理实现
```

### 6.6 实时演示 (`demo.py`)
```python
import cv2
from ultralytics import YOLO
model = YOLO('best.pt')
cap = cv2.VideoCapture(0)
while True:
    ret, frame = cap.read()
    results = model(frame, imgsz=1280, conf=0.4)
    annotated = results[0].plot()
    cv2.imshow('Traffic Sign Detection', annotated)
    if cv2.waitKey(1) & 0xFF == ord('q'):
        break
```

## 7. 评估指标
- mAP@0.5, mAP@0.5:0.95
- 小目标（面积<32×32）的 AP_S
- 推理速度 FPS（GPU/CPU）
- 与基线 YOLOv8s 对比实验表

## 8. 训练步骤
1. 运行 `prepare_dataset.py` 生成 YOLO 格式标签和 data.yaml。
2. 修改 `yolov8s_p2_eca.yaml` 确保正确。
3. 执行 `python train.py` 训练改进模型。
4. 执行基线 YOLOv8s 训练（使用官方 `yolov8s.yaml`）作为对比。
5. 运行 `python detect.py` 测试单张图片，观察小目标检测效果。
6. 运行 `python demo.py` 进行实时摄像头测试。

## 9. 预期成果
- 改进模型在 TT100K 子集上 mAP@0.5 达到 0.88+，小目标 AP_S 提升 5%+。
- 推理速度在 RTX 3060 上 > 40 FPS。
- 提供详细消融实验表（P2、ECA、Copy-Paste 各步贡献）。

## 10. 注意事项
- 由于 TT100K 图片分辨率极高，训练时可裁剪为 1280×1280 的瓦片以降低显存占用，但需相应调整标签坐标。
- ECA 的实现要确保与 YOLOv8 的 ONNX 导出兼容，避免使用不支持的操作用于后续部署（可选，若只是演示则可忽略）。
- Copy-Paste 增强需处理好坐标转换，避免粘贴标志严重偏离图像边界。
