% !TEX program = xelatex
% ═══════════════════════════════════════════════════════════════════════════════
% 中国大学生计算机设计大赛 — 人工智能实践赛作品报告
% 完全复刻 docs/作品报告模板.docx 格式
% 编译: xelatex report.tex
% ═══════════════════════════════════════════════════════════════════════════════

\documentclass[12pt,a4paper,linespread=1.5,fontset=none]{ctexart}

% ─── ctex 编号: 第一章、1.1、1.1.1（匹配 docx absNumId=0） ────────────────
\ctexset{
    section = {
        name = {第,章},
        number = \arabic{section},
    },
    subsection = {
        number = \arabic{section}.\arabic{subsection},
    },
    subsubsection = {
        number = \arabic{section}.\arabic{subsection}.\arabic{subsubsection},
    },
}

% ─── Packages ─────────────────────────────────────────────────────────────────
\usepackage[top=2.54cm, bottom=2.54cm, left=3.18cm, right=3.18cm, headheight=13.6pt]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{array}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{float}
\usepackage{fontspec}
\usepackage{titlesec}
\usepackage{indentfirst}

% ─── Fonts（匹配 docx: 宋体正文 / 黑体标题 / 华文中宋封面 / 华文楷体副标题）───
\setCJKmainfont{Songti SC}[BoldFont=Heiti SC]
\setCJKsansfont{Heiti SC}
\setCJKmonofont{STFangsong}
\setCJKfamilyfont{zhsong}{Songti SC}        % 宋体 → 正文
\setCJKfamilyfont{zhhei}{Heiti SC}           % 黑体 → 标题
\setCJKfamilyfont{zhkai}{Kaiti SC}           % 楷体 → 封面副标题
\setCJKfamilyfont{zhfs}{STFangsong}          % 仿宋
\setCJKfamilyfont{zhzongsong}{Songti SC}     % macOS 无华文中宋，宋体 Bold 替代

\newcommand{\songti}{\CJKfamily{zhsong}}
\newcommand{\heiti}{\CJKfamily{zhhei}}
\newcommand{\kaishu}{\CJKfamily{zhkai}}
\newcommand{\fangsong}{\CJKfamily{zhfs}}
\newcommand{\zongsong}{\CJKfamily{zhzongsong}}

% ─── Heading spacing（精确匹配 docx EMU 值）───────────────────────────────────
% H1: sb=46.8pt sa=7.8pt, 黑体 18pt Bold 居中
\titleformat{\section}{\centering\heiti\bfseries\fontsize{18pt}{27pt}\selectfont}{}{0em}{}
\titlespacing{\section}{0pt}{46.8pt}{7.8pt}

% H2: sb=23.4pt sa=4.7pt, 黑体 14pt Bold
\titleformat{\subsection}{\heiti\bfseries\fontsize{14pt}{21pt}\selectfont}{}{0em}{}
\titlespacing{\subsection}{0pt}{23.4pt}{4.7pt}

% H3: sb=15.6pt sa=3.1pt, 黑体 12pt Bold
\titleformat{\subsubsection}{\heiti\bfseries\fontsize{12pt}{18pt}\selectfont}{}{0em}{}
\titlespacing{\subsubsection}{0pt}{15.6pt}{3.1pt}

% ─── Body text ────────────────────────────────────────────────────────────────
% 正文段落: 宋体 12pt, 首行缩进 2 字符(≈24pt), 段前后 3.1pt
\setlength{\parindent}{2em}
\setlength{\parskip}{3.1pt plus1pt minus1pt}

% ─── Caption（图/表题注: sb=sa=4.7pt, 黑体 10pt）─────────────────────────────
\captionsetup[figure]{font={small,bf}, belowskip=4.7pt, aboveskip=4.7pt}
\captionsetup[table]{font={small,bf}, belowskip=4.7pt, aboveskip=4.7pt}

% ─── List spacing ─────────────────────────────────────────────────────────────
\setlist{nosep, leftmargin=2em, itemindent=0em}

% ─── Header ───────────────────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\fancyhead[C]{\small\kaishu 基于改进 YOLO26 的小目标交通标志实时检测}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

\begin{document}

% ═══════════════════════════════════════════════════════════════════════════════
% 封面（完全复刻 docx）
% ═══════════════════════════════════════════════════════════════════════════════
\begin{titlepage}
\thispagestyle{empty}
\begin{center}

\vspace*{3cm}

% 华文中宋 28pt Bold 居中
{\zongsong\bfseries\fontsize{28pt}{42pt}\selectfont 2025年（第18届）\par}

\vspace{0.5cm}

% 华文中宋 28pt Bold 两端对齐（DISTRIBUTE）
{\zongsong\bfseries\fontsize{28pt}{42pt}\selectfont 中国大学生计算机设计大赛\par}

\vspace{1.5cm}

% 华文楷体 20pt 居中，段前后 31.2pt
\vspace{31.2pt}
{\kaishu\fontsize{20pt}{30pt}\selectfont 人工智能实践赛作品报告\par}
\vspace{31.2pt}

\vspace{2cm}

% 宋体 16pt 左对齐，段前后 7.8pt
\begin{flushleft}
{\songti\fontsize{16pt}{24pt}\selectfont
\vspace{7.8pt}
作品编号：\underline{\hspace{6cm}}\\[7.8pt]
\vspace{7.8pt}
作品名称：\underline{\hspace{6cm}}\\[7.8pt]
\vspace{7.8pt}
填写日期：\underline{\hspace{6cm}}\\[7.8pt]
}
\end{flushleft}

\end{center}
\end{titlepage}

% ═══════════════════════════════════════════════════════════════════════════════
% 第1章  作品概述
% ═══════════════════════════════════════════════════════════════════════════════
\section{作品概述}

随着智能交通系统的快速发展，交通标志检测成为自动驾驶与辅助驾驶领域的核心技术之一。然而在实际道路场景中，远处的小尺寸交通标志（在图像中占比 $< 0.1\%$，面积 $< 32\times 32$ 像素）往往难以被通用目标检测器有效识别，导致漏检率居高不下。

本作品针对小目标交通标志检测这一痛点，基于 ultralytics 最新 YOLO26s 检测框架，设计了六阶段渐进式改进方案。YOLO26 是 ultralytics 在 2025 年发布的最新检测架构，相较上一代 YOLOv8 引入了 C3k2 多尺度核选择模块、C2PSA 交叉阶段自注意力、端到端检测模式（end2end + reg\_max=1）等关键升级，基线性能更优。在此基础上，本作品原创设计了三项针对性改进模块：(1) \textbf{ARF-Head（自适应感受野检测头）}：通过多分支空洞卷积与可学习 softmax 融合，为不同检测层提供尺度感知的自适应感受野；(2) \textbf{BCEM（双向上下文增强模块）}：在 Backbone 到 Neck 的跳跃连接处引入通道+空间双向注意力，显式增强前景区域并抑制背景噪声；(3) \textbf{HJ-Loss（分层联合损失函数）}：对小目标检测层使用 WIoUv3 + NWD 联合监督，对大目标层仅使用 WIoUv3，实现差异化的损失策略。

系统基于 TT100K 交通标志数据集（2021 版）进行训练与验证，选取 5 类典型小目标标志（限速 5、限速 30、限速 40、禁止驶入、禁止行人通行），通过严格的控制变量消融实验量化每个创新模块的独立贡献。系统支持图片、视频及实时摄像头输入。

\textbf{关键词：}小目标检测；交通标志识别；YOLO26；自适应感受野；双向上下文增强；分层联合损失

% ═══════════════════════════════════════════════════════════════════════════════
% 第2章  问题分析
% ═══════════════════════════════════════════════════════════════════════════════
\section{问题分析}

\subsection{问题来源}

随着自动驾驶技术从 L2 向 L3/L4 级别演进，环境感知系统对交通标志检测的精度和实时性提出了越来越高的要求。在实际驾驶场景中，车辆需要在远距离（50--100 米）处提前识别交通标志，而此时标志在图像中往往仅占 $16\times 16$ 至 $32\times 32$ 像素区域，属于典型的小目标检测问题。

现有检测框架（包括 YOLOv8、YOLO11、YOLO12 等）对小目标检测存在三个结构性缺陷：其一，最高分辨率检测层（P3，stride=8）的特征粒度不足以充分表达小目标——一个 $16\times 16$ 的标志在 $80\times 80$ 的 P3 特征图上仅占 $2\times 2$ 个特征点，极易被下采样过程中丢失的细节信息所淹没；其二，Neck 网络的跳跃连接直接将 Backbone 浅层特征引入 FPN/PAN 路径，但这些浅层特征包含大量与前景目标无关的背景纹理（道路标线、文字、建筑边缘等），小目标极易与之混淆；其三，所有检测层使用相同的损失函数和权重，未考虑不同尺度目标对定位精度的差异化需求——IoU Loss 对小目标的位置偏移极度敏感（2 像素偏移可使 IoU 从 0.8 骤降至 0.5），而对大目标几乎无影响。

\subsection{现有解决方案}

针对小目标检测问题，学术界与工业界主要提出了以下几类解决方案：

\begin{enumerate}[label=(\arabic*)]
    \item \textbf{多尺度特征融合}：FPN \cite{lin2017fpn} 及 PANet、BiFPN 等通过自顶向下和自底向上的路径增强多尺度特征表达。ultralytics 官方已提供 YOLO26s-P2 变体（yolo26s-p2.yaml），在 Neck 中增加一次上采样与下采样以引入 P2/4 高分辨率检测头，但对 P2 层仅做了通道和结构的简单扩展。
    \item \textbf{注意力机制}：SE-Net、CBAM、ECA-Net \cite{wang2020eca} 通过通道或空间注意力增强关键特征。YOLO26 自带的 C2PSA 模块在 Backbone 末端提供全局交叉阶段自注意力，但仅覆盖全局语义层面，缺乏针对前景/背景边界的局部判别能力。
    \item \textbf{损失函数}：CIoU、DIoU、SIoU 等 IoU 变体在通用场景下改进了回归精度。WIoUv3 \cite{tong2023wiou} 通过动态非单调聚焦机制自动关注中等质量样本、抑制极端离群值；NWD \cite{wang2021nwd} 将边界框建模为 2D 高斯分布并计算 Wasserstein 距离，具有尺度不敏感的优势，特别适合小目标定位。
    \item \textbf{后处理优化}：Soft-NMS \cite{bodla2017soft} 以置信度衰减替代硬删除，在密集目标场景中能保留更多的真阳性预测，减少小目标的误删除。
\end{enumerate}

\subsection{本作品要解决的痛点问题}

\begin{enumerate}[label=(\arabic*)]
    \item \textbf{感受野与检测尺度不匹配}：标准 YOLO26 所有检测头使用完全相同的结构（C3k2 + Detect），未考虑不同尺度检测层对感受野需求的差异。P2 层检测极小目标时需要适度扩大感受野以捕获上下文（区分目标与噪声），而 P5 层检测大目标时感受野已足够甚至过大。
    \item \textbf{跳跃连接噪声污染}：PANet 的跳跃连接直接将 Backbone 浅层特征拼接到 Neck，浅层特征包含大量背景纹理，小目标极易与之混淆。目前缺乏在跳跃连接处对 Backbone 特征进行显式前景/背景提纯的机制。
    \item \textbf{损失函数缺乏尺度差异化}：默认 CIoU Loss 对所有检测层统一使用，对小目标的位置偏移极度敏感。NWD Loss 虽对尺度不敏感但单独使用会牺牲大目标定位精度。需要一个能按检测层自适应调整的联合损失策略。
    \item \textbf{密集场景误删除}：城市道路中交通标志常成组出现，标准 NMS 的硬删除策略容易误删邻近的低置信度真阳性框。
\end{enumerate}

% ═══════════════════════════════════════════════════════════════════════════════
% 第3章  技术方案
% ═══════════════════════════════════════════════════════════════════════════════
\section{技术方案}

\subsection{总体技术路线}

本作品以 YOLO26s 为基线检测框架，采用六阶段递进式改进策略（图~\ref{fig:pipeline}），构建面向小目标交通标志的实时检测系统。六个阶段依次为：Stage 1 建立 YOLO26s 性能基线、Stage 2 采用官方 P2 高分辨率检测变体、Stage 3 集成原创 ARF-Head 自适应感受野检测头、Stage 4 集成原创 BCEM 双向上下文增强模块、Stage 5 引入原创 HJ-Loss 分层联合损失函数、Stage 6 全模块集成与 Soft-NMS 推理优化。

\begin{figure}[H]
\centering
\fbox{\begin{minipage}{0.85\textwidth}
\vspace{3cm}
\centering
\songti （此处插入系统总体技术路线框图）
\vspace{3cm}
\end{minipage}}
\caption{系统总体技术路线（六阶段递进式改进）}
\label{fig:pipeline}
\end{figure}

\subsection{基线模型：YOLO26s}

YOLO26 是 ultralytics 在 YOLOv8/v10/v11/v12 之后发布的下一代检测架构，相较 YOLOv8 引入三项关键升级：

\begin{enumerate}[label=(\arabic*)]
    \item \textbf{C3k2 模块}：替代 YOLOv8 的 C2f，通过 Kernel Selection 机制实现多尺度特征自适应融合，对小目标的细粒度特征提取更有效；
    \item \textbf{C2PSA 注意力}：在 Backbone 末端内置 Cross-Stage Partial Self-Attention，提供全局上下文建模，优于事后插入的 ECA/SE 等轻量注意力；
    \item \textbf{端到端检测模式}（end2end + reg\_max=1）：去掉 DFL 分布预测分支，直接回归边界框，减少小目标定位的量化误差。
\end{enumerate}

YOLO26s（small 变体）参数量约 10.01M，GFLOPs 约 22.8，在 COCO 上 mAP@0.5:0.95 约为 45.8\%（640 分辨率），综合性能优于同体量的 YOLOv8s。本作品选择 YOLO26s 作为基线模型，在其基础上进行针对性改进。

\subsection{Stage 2：增加 P2 高分辨率检测层}

YOLO26 默认检测头包含 P3（下采样 8 倍，$80\times 80$）、P4（$40\times 40$）、P5（$20\times 20$）三层。本阶段采用 ultralytics 官方提供的 yolo26s-p2.yaml 变体，在 Neck 的 FPN 路径中增加一层上采样，将 P3 特征进一步放大至 P2 级别（下采样 4 倍，$160\times 160$），与 Backbone 第 2 层（stride=4）经 C3k2 融合后，构成第四个检测头。P2 层的每个空间位置对应 $4\times 4$ 像素区域，$16\times 16$ 的小目标在该层占据 $4\times 4$ 个特征点（P3 仅 $2\times 2$），特征表达能力翻倍。

YOLO26s-P2 参数量约 9.77M，GFLOPs 约 27.8，stride 配置为 $[4, 8, 16, 32]$。

\begin{figure}[H]
\centering
\fbox{\begin{minipage}{0.8\textwidth}
\vspace{2.5cm}
\centering
\songti （此处插入 YOLO26s-P2 四检测头结构示意图）
\vspace{2.5cm}
\end{minipage}}
\caption{YOLO26s-P2 四尺度检测头结构示意（P2 层 stride=4）}
\label{fig:p2}
\end{figure}

\subsection{Stage 3：ARF-Head 自适应感受野检测头（原创）}

\subsubsection{设计动机}

YOLO26 的四检测头使用相同的 C3k2 + Detect 结构，未考虑不同尺度检测层对感受野需求的差异。P2 层检测极小目标时需要略大的感受野以捕获上下文（帮助区分目标与背景噪声），而 P5 层检测大目标时感受野已经足够甚至过大。本作品原创设计 ARF-Head，为每个检测层提供自适应感受野。

\subsubsection{模块结构}

ARF（Adaptive Receptive Field）模块插入在每个检测层的 C3k2 之后、Detect 之前，包含三个并行的空洞深度可分离卷积分支：

\begin{itemize}
    \item \textbf{多分支空洞卷积}：每个分支包含一个 $3\times 3$ 深度可分离空洞卷积（depthwise）加 $1\times 1$ 逐点卷积（pointwise），空洞率根据检测层自适应配置：
    \begin{itemize}
        \item P2（stride=4）：空洞率 $[1, 3, 5]$，适度扩大感受野以捕获小目标周围上下文
        \item P3（stride=8）：空洞率 $[1, 2, 3]$，轻度扩大感受野
        \item P4（stride=16）：空洞率 $[1, 1, 2]$，接近标准卷积
        \item P5（stride=32）：空洞率 $[1, 1, 1]$，标准卷积（大目标已有足够感受野）
    \end{itemize}
    \item \textbf{可学习 softmax 融合}：通过全局平均池化 $\to$ 全连接 $\to$ softmax 生成各分支的动态权重，替代简单的等权加和，使网络自主学习最优的感受野组合；
    \item \textbf{残差连接}：最终输出 = 投影（加权融合）+ 输入，保证模块插入后的训练稳定性。
\end{itemize}

ARF 模块使用深度可分离卷积控制计算开销，四个 ARF 模块的总参数量增量约 0.16M，对推理速度的影响极小。

\subsection{Stage 4：BCEM 双向上下文增强模块（原创）}

\subsubsection{设计动机}

PANet 的跳跃连接直接将 Backbone 浅层特征拼接到 Neck，但这些浅层特征包含大量与前景目标无关的背景纹理。小目标（如交通标志）极易与道路标志线、文字、建筑边缘等背景元素混淆。YOLO26 的 C2PSA 仅提供全局自注意力，缺乏对前景/背景边界的显式局部判别能力。

\subsubsection{模块结构}

BCEM（Bi-directional Context Enhancement Module）插入在 Backbone 到 Neck 的每条跳跃连接之前（FPN 路径中共 3 处：P4→Neck、P3→Neck、P2→Neck），对 Backbone 特征进行``提纯''后再送入 Neck 拼接。每个 BCEM 包含双分支并行处理：

\begin{itemize}
    \item \textbf{通道分支}：全局平均池化 $\to$ 自适应核 1D 卷积 $\to$ Sigmoid。1D 卷积核大小 $k = |\frac{\log_2 C}{\gamma} + \frac{b}{\gamma}|_{\text{odd}}$（$\gamma=2, b=1$），自适应捕获局部跨通道交互，保留对小目标检测贡献大的通道。
    \item \textbf{空间分支}：$1\times 1$ 卷积压缩 $\to$ $7\times 7$ 大核深度可分离卷积 $\to$ $1\times 1$ 卷积 $\to$ Sigmoid。$7\times 7$ 大核提供足够的感受野进行前景/背景判别，生成空间显著性图。
    \item \textbf{乘法融合}：双分支输出通过逐元素乘法融合（element-wise multiplication），确保通道和空间两个维度双重确认才增强，比加法融合更保守、更精准。
    \item \textbf{残差连接}：$\text{Output} = \text{Input} + \text{Input} \times \text{ChAttn} \times \text{SpAttn}$。初始化时空间分支末层权重置为零，使模块从恒等映射起步，训练过程中逐步学会增强前景区域。
\end{itemize}

三个 BCEM 模块的总参数量增量约 0.07M，几乎不影响推理速度。

\subsection{Stage 5：HJ-Loss 分层联合损失函数（原创）}

\subsubsection{设计动机}

IoU-based Loss 对小目标的微小位置偏移极度敏感——$16\times 16$ 的目标偏移 2 个像素，IoU 可能从 0.8 骤降至 0.5。而大目标（如 $100\times 100$）偏移 2 个像素影响极小。此外，不同检测层负责不同尺度的目标，但 YOLO26 默认对所有层使用相同的 CIoU Loss 和统一权重。本作品原创设计 HJ-Loss（Hierarchical Joint Loss），实现检测层级别的差异化损失策略。

\subsubsection{损失函数组成}

\textbf{组件一：主损失\quad Wise-IoU v3}

WIoUv3 \cite{tong2023wiou} 替换默认的 CIoU Loss 作为所有检测层的主损失函数。其核心机制是通过离群度 $\beta = L_{\text{IoU}}^* / \overline{L_{\text{IoU}}}$（以 running mean 归一化）动态调整每个样本的梯度增益：
\begin{equation}
r = \frac{\beta}{\delta \cdot \alpha^{\beta - \delta}}
\end{equation}
其中 $\delta=2.0$ 为聚焦强度参数。$\beta$ 小（高质量 anchor）→ 正常梯度；$\beta$ 中等（需关注的 anchor）→ 放大梯度；$\beta$ 大（极端离群）→ 抑制梯度。

结合中心点距离惩罚项 $R_{\text{WIoU}} = \exp\left(\frac{(x - x_{gt})^2 + (y - y_{gt})^2}{W_g^2 + H_g^2}\right)$，最终：
\begin{equation}
L_{\text{WIoUv3}} = r \cdot R_{\text{WIoU}} \cdot L_{\text{IoU}} \quad (\text{梯度计算时 } r \text{ detach})
\end{equation}

\textbf{组件二：辅助损失\quad NWD（Normalized Wasserstein Distance）}

NWD \cite{wang2021nwd} 将边界框建模为 2D 高斯分布 $\mathcal{N}(\mu, \Sigma)$，其中 $\mu = (cx, cy)$，$\Sigma = \text{diag}(w^2/4, h^2/4)$。计算两个高斯分布之间的 Wasserstein 距离并归一化：
\begin{equation}
\text{NWD}(\mathcal{N}_a, \mathcal{N}_b) = \exp\left(-\frac{\sqrt{W_2^2(\mathcal{N}_a, \mathcal{N}_b)}}{C}\right)
\end{equation}
其中 $W_2^2 = ||\mu_a - \mu_b||^2 + ||\Sigma_a^{1/2} - \Sigma_b^{1/2}||_F^2$，归一化常数 $C=12.0$（匹配 TT100K 数据集平均小目标尺寸）。

NWD 的核心优势在于：对小目标的微小位置偏移不敏感（平滑的高斯距离度量），但对大尺寸目标的定位精度不如 IoU Loss。

\textbf{组件三：分层自适应权重}

针对 NWD 对大目标不敏感的特性，HJ-Loss 采用分层策略：
\begin{itemize}
    \item P2 和 P3 层（小目标检测层）：$L = L_{\text{WIoUv3}} + \lambda_{\text{nwd}} \cdot L_{\text{NWD}}$，其中 $\lambda_{\text{nwd}} = 0.3$
    \item P4 和 P5 层（大目标检测层）：$L = L_{\text{WIoUv3}}$（不加 NWD，避免对大目标引入噪声）
\end{itemize}

总损失函数为：
\begin{equation}
L_{\text{total}} = \lambda_{\text{cls}} L_{\text{cls}} + \sum_{\text{layer} \in \{\text{P2,P3}\}} \left(L_{\text{WIoUv3}} + 0.3 \cdot L_{\text{NWD}}\right) + \sum_{\text{layer} \in \{\text{P4,P5}\}} L_{\text{WIoUv3}}
\end{equation}

\subsection{Stage 6：后处理优化与系统集成}

最终集成阶段采用 Gaussian Soft-NMS \cite{bodla2017soft} 替代标准 NMS，以高斯衰减替代硬删除：
\begin{equation}
s_i = s_i \cdot \exp\left(-\frac{\text{IoU}(M, b_i)^2}{\sigma}\right), \quad \sigma = 0.5
\end{equation}
在密集交通标志场景中（如路口多标志并排），Soft-NMS 可有效保留邻近的真阳性预测，降低小目标的误删除率。可选配置包括多尺度 TTA（$[0.83\times, 1.0\times, 1.17\times]$ 三个尺度 + WBF 融合）和 ONNX 导出以支持边缘设备部署。

% ═══════════════════════════════════════════════════════════════════════════════
% 第4章  系统实现
% ═══════════════════════════════════════════════════════════════════════════════
\section{系统实现}

\subsection{数据集}

本作品采用 TT100K 2021 版数据集，原始数据包含约 100,000 张腾讯街景全景图，标注约 30,000 个交通标志实例，涵盖 221 类中国交通标志。筛选其中 5 类典型小目标标志进行实验，数据集类别分布如表~\ref{tab:dataset} 所示。

\begin{table}[H]
\centering
\caption{数据集类别分布（TT100K 子集）}
\begin{tabular}{cccc}
\toprule
\textbf{类名} & \textbf{TT100K 标识} & \textbf{中文含义} & \textbf{标注数} \\
\midrule
speed\_limit\_5  & i2   & 限速 5 km/h      & 217  \\
speed\_limit\_30 & i4   & 限速 30 km/h     & 362  \\
speed\_limit\_40 & i5   & 限速 40 km/h     & 794  \\
no\_entry        & pne  & 禁止驶入         & 1120 \\
no\_pedestrians  & p10  & 禁止行人通行     & 193  \\
\bottomrule
\end{tabular}
\label{tab:dataset}
\end{table}

数据集按 7:2:1 划分为训练集（2171 张）、验证集（622 张）和测试集（314 张），固定随机种子 seed=42 保证可复现。训练时图像统一 resize 至 $640\times 640$，标注格式由原始 TT100K JSON 转换为 YOLO 归一化格式。数据预处理脚本支持三种不同的 JSON 标注格式自动检测（嵌套对象式、imgs+anns 分离式、COCO 格式）。

\subsection{实验环境}

\begin{table}[H]
\centering
\caption{实验环境配置}
\begin{tabular}{ll}
\toprule
\textbf{项目} & \textbf{配置} \\
\midrule
操作系统  & Ubuntu 22.04 / macOS 26.6 \\
GPU       & NVIDIA RTX 4060 (8GB) / Apple M5 \\
深度学习框架 & PyTorch 2.x + Ultralytics 8.x（内置 YOLO26） \\
Python    & 3.12 \\
CUDA      & 12.6（Linux/Windows） \\
\bottomrule
\end{tabular}
\label{tab:env}
\end{table}

\subsection{训练策略}

所有实验统一采用以下训练配置以确保可比性：输入分辨率 $640\times 640$，SGD 优化器（momentum=0.937，weight\_decay=0.0005），初始学习率 0.01，余弦退火调度（lrf=0.01），前 3 轮线性 warm-up（warmup\_momentum=0.8），训练 100 轮，早停 patience=20。数据增强采用 Mosaic（概率 1.0）、随机缩放（0.5）、平移（0.1）、HSV 抖动（h=0.015, s=0.7, v=0.4）和水平翻转（0.5）。最后 10 轮关闭 Mosaic 增强以提升收敛质量。不使用 mixup 和 copy\_paste。

Batch size 方面，所有 Stage 统一使用 batch=8（受限于 GPU 显存），使用相同的随机种子和优化器配置，严格保持控制变量——每阶段仅变更目标改进点，其余超参数完全一致。

\subsection{消融实验设计}

为量化各改进点的独立贡献，设计消融实验矩阵如表~\ref{tab:ablation} 所示，遵循严格的递进式控制变量原则。

\begin{table}[H]
\centering
\caption{消融实验矩阵（六阶段递进式）}
\begin{tabular}{cccccc}
\toprule
\textbf{实验} & \textbf{P2检测层} & \textbf{ARF-Head} & \textbf{BCEM} & \textbf{HJ-Loss} & \textbf{Soft-NMS} \\
\midrule
A (基线)    & $\times$ & $\times$ & $\times$ & $\times$ & $\times$ \\
B           & $\checkmark$ & $\times$ & $\times$ & $\times$ & $\times$ \\
C           & $\checkmark$ & $\checkmark$ & $\times$ & $\times$ & $\times$ \\
D           & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\times$ & $\times$ \\
E           & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\times$ \\
F (完整)    & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ & $\checkmark$ \\
\bottomrule
\end{tabular}
\label{tab:ablation}
\end{table}

贡献度分析公式：$\Delta_{\text{P2}} = \text{B} - \text{A}$、$\Delta_{\text{ARF}} = \text{C} - \text{B}$、$\Delta_{\text{BCEM}} = \text{D} - \text{C}$、$\Delta_{\text{HJ-Loss}} = \text{E} - \text{D}$、$\Delta_{\text{Soft-NMS}} = \text{F} - \text{E}$。

\subsection{系统功能模块}

系统以模块化 Python 脚本实现，主要功能模块包括：

\begin{itemize}
    \item \textbf{数据处理模块}（\texttt{data/prepare\_dataset.py}）：支持三种 JSON 格式自动检测与 YOLO 格式转换，自动筛选 5 类目标，7:2:1 随机划分；
    \item \textbf{模型定义模块}（\texttt{models/}）：包含 ARF-Head（\texttt{arf\_head.py}）、BCEM（\texttt{bcem.py}）、HJ-Loss（\texttt{hj\_loss.py}）的完整实现，以及 YAML 模型配置文件，通过 \texttt{model\_builder.py} 注册到 ultralytics 模块命名空间；
    \item \textbf{训练模块}（\texttt{scripts/train.py}）：YAML 配置驱动，CLI 参数覆写，支持所有 6 个训练阶段；
    \item \textbf{评估模块}（\texttt{scripts/eval.py}）：COCO 风格 mAP 评估，按小/中/大目标分组统计 AP\_S/AP\_M/AP\_L，支持与基线模型的对比分析；
    \item \textbf{推理模块}（\texttt{scripts/detect.py} / \texttt{scripts/demo.py}）：支持图片、视频、实时摄像头输入，含 FPS 显示与截图功能。
\end{itemize}

% ═══════════════════════════════════════════════════════════════════════════════
% 第5章  测试分析
% ═══════════════════════════════════════════════════════════════════════════════
\section{测试分析}

\subsection{Stage 1：YOLO26s 基线模型训练}

基线 YOLO26s 在 TT100K 子集上训练 100 轮（最终收敛 epoch = 100），训练过程中的损失与验证指标曲线如图~\ref{fig:stage1_results} 所示。

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{images/stage1/results.png}
\caption{Stage 1（YOLO26s 基线）训练曲线：box\_loss、cls\_loss 与 mAP@0.5 / mAP@0.5:0.95、Precision / Recall 随 epoch 变化}
\label{fig:stage1_results}
\end{figure}

训练在第 99 轮达到最佳 mAP@0.5（0.544），第 100 轮达到最佳 mAP@0.5:0.95（0.408）。训练集与验证集的损失在第 80 轮后趋于平稳，验证集损失未出现明显反弹，表明模型未过拟合。最终收敛指标如表~\ref{tab:baseline} 所示。

\begin{table}[H]
\centering
\caption{Stage 1 基线模型训练结果（YOLO26s，640×640，100 epoch）}
\begin{tabular}{lc}
\toprule
\textbf{指标} & \textbf{数值} \\
\midrule
mAP@0.5      & 0.544 \\
mAP@0.5:0.95 & 0.408 \\
Precision    & 0.601 \\
Recall       & 0.503 \\
Params       & 10.01M \\
GFLOPs       & 22.8 \\
\bottomrule
\end{tabular}
\label{tab:baseline}
\end{table}

注：以上指标由 ultralytics 训练阶段自动验证得出（验证集，输入分辨率 640×640）。正式的 COCO 风格评估（含 AP\_S/AP\_M/AP\_L 三类目标尺寸分组统计）需在测试集上以更高分辨率（imgsz=1280）单独运行，命令如下：

\begin{verbatim}
python scripts/eval.py \
    --weights experiments/stage1_baseline/train/weights/best.pt \
    --data data/processed/dataset.yaml \
    --split test --imgsz 1280 --device 0 --analyze_sizes
\end{verbatim}

\subsection{Stage 1：模型评估与分析}

\subsubsection{混淆矩阵}

归一化混淆矩阵如图~\ref{fig:stage1_cm} 所示。对角线元素反映各分类的正确预测比例。``no\_entry''（禁止驶入，class 3）由于训练样本最多（1120 个标注实例），分类准确率最高；``no\_pedestrians''（禁止行人通行，class 4）、``speed\_limit\_5''（限速 5，class 0）两类因样本稀疏（分别为 193 和 217），存在一定的误分。整体来看，模型未出现严重的类间混淆，背景误检率控制在合理范围内。

\begin{figure}[H]
\centering
\includegraphics[width=0.65\textwidth]{images/stage1/confusion_matrix.png}
\caption{Stage 1 归一化混淆矩阵（验证集）}
\label{fig:stage1_cm}
\end{figure}

\subsubsection{Precision-Recall 与 F1 曲线}

PR 曲线（图~\ref{fig:stage1_pr}）与 F1-Confidence 曲线（图~\ref{fig:stage1_f1}）显示：mAP@0.5 达到 0.544，各类别的 PR 曲线在低召回率区域（Recall $< 0.4$）保持较高精度。F1 曲线表明最优置信度阈值约在 0.2--0.4 区间，其中``speed\_limit\_40''（class 2）因样本量最大（794 个标注），F1 得分最高（约 0.55）。

\begin{figure}[H]
\centering
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{images/stage1/PR_curve.png}
\caption{Stage 1 Precision-Recall 曲线（各类别 + 全类 mAP@0.5）}
\label{fig:stage1_pr}
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{images/stage1/F1_curve.png}
\caption{Stage 1 F1-Confidence 曲线（各类别 + 全类）}
\label{fig:stage1_f1}
\end{minipage}
\end{figure}

\subsubsection{检测效果可视化}

图~\ref{fig:stage1_val_pred} 展示了验证集上的检测结果（置信度阈值 0.25）。YOLO26s 基线能够检测出大部分交通标志，但在以下场景存在不足：(1) 极小尺寸标志（$< 16\times 16$ px）漏检；(2) 密集排列标志之间出现 NMS 误删除；(3) 部分被遮挡或光照不良的标志置信度偏低（$< 0.3$）。这些问题正是后续 Stage 2--5 改进方案的着力点。

\begin{figure}[H]
\centering
\includegraphics[width=0.55\textwidth]{images/stage1/val_pred0.jpg}
\hfill
\includegraphics[width=0.55\textwidth]{images/stage1/val_pred1.jpg}
\caption{Stage 1 验证集检测效果示例（YOLO26s 基线，conf=0.25）}
\label{fig:stage1_val_pred}
\end{figure}

\subsection{Stage 2：YOLO26s-P2 高分辨率检测层}

采用官方 yolo26s-p2.yaml 变体，增加 P2/4 高分辨率检测层（160×160 特征图）。训练配置与 Stage 1 保持一致（640×640，100 epoch，batch=8）。训练曲线如图~\ref{fig:stage2_results} 所示。

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{images/stage2/results.png}
\caption{Stage 2（YOLO26s-P2）训练曲线}
\label{fig:stage2_results}
\end{figure}

\begin{table}[H]
\centering
\caption{Stage 2 模型训练结果（YOLO26s-P2，640×640，100 epoch）}
\begin{tabular}{lccc}
\toprule
\textbf{指标} & \textbf{Stage 1 (基线)} & \textbf{Stage 2 (+P2)} & $\Delta$ \\
\midrule
mAP@0.5      & 0.544 & 0.552 & +0.008 \\
mAP@0.5:0.95 & 0.408 & 0.425 & +0.017 \\
Precision    & 0.601 & 0.588 & $-$0.013 \\
Recall       & 0.503 & 0.504 & +0.001 \\
Params       & 10.01M & 9.77M & $-$0.24M \\
GFLOPs       & 22.8 & 27.8 & +5.0 \\
\bottomrule
\end{tabular}
\label{tab:stage2}
\end{table}

P2 检测层带来了 mAP@0.5:0.95 的提升（+0.017），说明在更高 IoU 阈值下定位精度有所改善。但 Precision 微降（$-$0.013），Recall 几乎持平（+0.001），整体增益不如预期强烈。可能原因：(1) 训练分辨率为 640×640，P2 层（stride=4）的 160×160 特征图在此分辨率下仍对极小目标分辨力不足；(2) 5 类目标在 TT100K 中的平均尺寸极度偏小，P2 单独引入的收益受限，后续 Stage 3--5 的注意力机制与分层损失预计能进一步释放 P2 层的潜力。

\subsubsection{混淆矩阵对比}

\begin{figure}[H]
\centering
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{images/stage1/confusion_matrix.png}
\caption{Stage 1 混淆矩阵}
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{images/stage2/confusion_matrix.png}
\caption{Stage 2 混淆矩阵}
\end{minipage}
\label{fig:cm_comparison}
\end{figure}

Stage 2 的混淆矩阵（图~\ref{fig:cm_comparison}）与 Stage 1 整体相似，对角线占优。``no\_entry''（class 3）仍为分类最准确的类别，``speed\_limit\_5''（class 0）和``no\_pedestrians''（class 4）因训练样本少仍存在背景误分。

\subsubsection{Precision-Recall 曲线}

\begin{figure}[H]
\centering
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{images/stage2/PR_curve.png}
\caption{Stage 2 Precision-Recall 曲线}
\label{fig:stage2_pr}
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{images/stage2/F1_curve.png}
\caption{Stage 2 F1-Confidence 曲线}
\label{fig:stage2_f1}
\end{minipage}
\end{figure}

\subsection{消融实验汇总}

\begin{table}[H]
\centering
\caption{消融实验结果汇总（A、B 为实测，C--F 待训练）}
\begin{tabular}{lccccc}
\toprule
\textbf{实验} & \textbf{模型配置} & \textbf{mAP@0.5} & \textbf{mAP@0.5:0.95} & \textbf{AP\_S} & \textbf{状态} \\
\midrule
A & YOLO26s                     & 0.544 & 0.408  & 待评估 & ✅ 已完成 \\
B & + P2                        & 0.552 & 0.425  & 待评估 & ✅ 已完成 \\
C & + ARF-Head                  & —     & —      & —      & ⬜ 待训练 \\
D & + BCEM                      & —     & —      & —      & ⬜ 待训练 \\
E & + HJ-Loss                   & —     & —      & —      & ⬜ 待训练 \\
F & + Soft-NMS                  & —     & —      & —      & ⬜ 待训练 \\
\bottomrule
\end{tabular}
\label{tab:ablation_results}
\end{table}

$\Delta_{\text{P2}}$ = B $-$ A：mAP@0.5 +0.008，mAP@0.5:0.95 +0.017。

\subsection{Stage 3--6：后续阶段计划}

\begin{table}[H]
\centering
\caption{剩余阶段改进计划}
\begin{tabular}{clcc}
\toprule
\textbf{阶段} & \textbf{改进模块} & \textbf{创新程度} & \textbf{目标} \\
\midrule
Stage 3 & ARF-Head 自适应感受野检测头 & ⭐⭐⭐ 原创 & 提升小目标上下文感知能力 \\
Stage 4 & BCEM 双向上下文增强模块   & ⭐⭐⭐⭐ 原创 & 抑制背景噪声，增强前景判别 \\
Stage 5 & HJ-Loss 分层联合损失函数   & ⭐⭐⭐⭐ 原创 & 小目标定位精度提升 \\
Stage 6 & Soft-NMS + 系统集成        & 集成优化   & 密集场景召回提升 \\
\bottomrule
\end{tabular}
\label{tab:stage_plan}
\end{table}

% ═══════════════════════════════════════════════════════════════════════════════
% 第6章  作品总结
% ═══════════════════════════════════════════════════════════════════════════════
\section{作品总结}

\subsection{作品特色与创新点}

\begin{enumerate}[label=(\arabic*)]
    \item \textbf{最新基线模型}：采用 ultralytics 2025 年发布的 YOLO26s，自带 C3k2 多尺度核选择、C2PSA 全局自注意力、端到端检测模式（end2end + reg\_max=1）三大升级，基线性能显著优于上一代 YOLOv8s；
    \item \textbf{ARF-Head（原创）}：自适应感受野检测头，通过多分支空洞卷积 + 可学习 softmax 融合，首次为不同检测层提供尺度感知的自适应感受野，参数量仅增 0.16M；
    \item \textbf{BCEM（原创）}：双向上下文增强模块，在 Backbone→Neck 跳跃连接处引入通道+空间双向注意力，乘法融合 + 残差连接实现精准的前景/背景判别，参数量仅增 0.07M；
    \item \textbf{HJ-Loss（原创）}：分层联合损失函数，首次提出检测层级别的差异化损失策略——P2/P3 小目标层使用 WIoUv3 + NWD 联合监督，P4/P5 大目标层仅使用 WIoUv3，实现``不同尺度、不同策略''；
    \item \textbf{严谨的消融实验}：六阶段控制变量递进式设计，每个改进点的独立贡献完全可量化（$\Delta_{\text{P2}}$、$\Delta_{\text{ARF}}$、$\Delta_{\text{BCEM}}$、$\Delta_{\text{HJ-Loss}}$、$\Delta_{\text{Soft-NMS}}$）；
    \item \textbf{工程化设计}：YAML 配置驱动 + CLI 覆写，自定义模块通过 ultralytics 注册机制无缝集成（无需修改框架源码），支持 Linux/Windows/Mac 一键训练。
\end{enumerate}

\subsection{应用推广}

\begin{itemize}
    \item \textbf{车载辅助驾驶}：嵌入车载计算平台，为驾驶员提供实时交通标志提示，远距离（50--100 米）提前预警；
    \item \textbf{交通监控}：部署于路边摄像头，自动检测并统计交通标志状态，辅助道路设施维护；
    \item \textbf{高精地图更新}：结合 SLAM 定位，批量采集道路标志信息用于地图更新与验证；
    \item \textbf{智能驾考}：自动评判驾考过程中学员对交通标志的识别与响应。
\end{itemize}

\subsection{作品展望}

\begin{enumerate}[label=(\arabic*)]
    \item \textbf{模型轻量化}：探索知识蒸馏或结构化剪枝，进一步降低模型计算开销（当前 P2 变体 FLOPs 增加约 25\%），达到 Jetson Nano 等边缘设备实时运行要求；
    \item \textbf{多天气鲁棒性}：引入雾天、雨夜、逆光等极端场景的数据增强或域自适应方法，提升全天候检测鲁棒性；
    \item \textbf{多帧时序融合}：利用视频时序信息进行检测结果跟踪与平滑（如 ByteTrack 等轻量跟踪器），减少单帧漏检和误检；
    \item \textbf{端到端部署}：导出 ONNX → TensorRT 模型，结合 FP16/INT8 量化实现边缘端 $>$ 60 FPS 实时推理；
    \item \textbf{扩展类别覆盖}：将 TT100K 的全部 45 类标志纳入实验，验证方案在更大类别空间下的泛化能力。
\end{enumerate}

% ═══════════════════════════════════════════════════════════════════════════════
% 参考文献
% ═══════════════════════════════════════════════════════════════════════════════
\begin{thebibliography}{99}

\bibitem{yolo26}
Ultralytics,
``YOLO26: Real-Time Object Detection,''
2025. [Online]. Available: \url{https://docs.ultralytics.com/models/yolo26}

\bibitem{lin2017fpn}
T.-Y. Lin, P. Doll\'{a}r, R. Girshick, K. He, B. Hariharan, and S. Belongie,
``Feature Pyramid Networks for Object Detection,''
in \textit{Proc. IEEE CVPR}, 2017, pp. 2117--2125.

\bibitem{wang2020eca}
Q. Wang, B. Wu, P. Zhu, P. Li, W. Zuo, and Q. Hu,
``ECA-Net: Efficient Channel Attention for Deep Convolutional Neural Networks,''
in \textit{Proc. IEEE CVPR}, 2020, pp. 11534--11542.

\bibitem{tong2023wiou}
Z. Tong, Y. Chen, Z. Xu, and R. Yu,
``Wise-IoU: Bounding Box Regression Loss with Dynamic Focusing Mechanism,''
\textit{arXiv preprint arXiv:2301.10051}, 2023.

\bibitem{wang2021nwd}
J. Wang, C. Xu, W. Yang, and L. Yu,
``A Normalized Gaussian Wasserstein Distance for Tiny Object Detection,''
\textit{arXiv preprint arXiv:2110.13389}, 2021.

\bibitem{bodla2017soft}
N. Bodla, B. Singh, R. Chellappa, and L. S. Davis,
``Soft-NMS --- Improving Object Detection with One Line of Code,''
in \textit{Proc. IEEE ICCV}, 2017, pp. 5561--5569.

\bibitem{tt100k}
Z. Zhu, D. Liang, S. Zhang, X. Huang, B. Li, and S. Hu,
``Traffic-Sign Detection and Classification in the Wild,''
in \textit{Proc. IEEE CVPR}, 2016, pp. 2110--2118.

\end{thebibliography}

\end{document}
